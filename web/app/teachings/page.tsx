import StubPage from "@/components/StubPage";

export const metadata = { title: "Seasonal Teachings — OMI" };

export default function Teachings() {
  return (
    <StubPage
      eyebrow="Seasonal Teachings"
      title="Deep-dive intensives, every season."
      lede="Multi-day teaching intensives, roughly every two months, built to take leaders deeper into scripture, culture, and calling."
      accent="teal"
    />
  );
}
