import StubPage from "@/components/StubPage";

export const metadata = { title: "Give — OMI" };

export default function Give() {
  return (
    <StubPage
      eyebrow="Partner with us"
      title="Fuel the Outpouring."
      script="Igniting Futures."
      lede="Every teaching, every trip, every student — made possible by partners who believe transformation is worth the investment."
      accent="gold"
    />
  );
}
